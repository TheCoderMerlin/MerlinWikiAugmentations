class Arr {
    /**
     * Get the last element of an array
     *
     * @param  {array} a The array
     * @return {any}
     */
    static last(a) {
        return a[a.length - 1];
    }
}

export default Arr;